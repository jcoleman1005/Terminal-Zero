# Status Strip Agent Prompt

You are Agent C: Status Strip.

Read:
- `docs/ui/TERMINAL_ZERO_UI_SPEC.md`
- `docs/ui/CONTRACTS.md`
- `docs/ui/AGENT_STATUS.md`

Work only inside your assigned status directories.

Implement ONLY the first Status milestone:
- standalone test scene
- six permanent subsystem slots
- fake SubsystemStatus inputs
- raw value + interpreted state
- severity rendering
- no-data state
- compact labels for narrow widths

Do not implement game-state calculations or major visual effects.

Do not modify shared contracts.

If the contract blocks implementation, create a contract-change request in your owned test/docs area.

Run the acceptance tests in `AGENT_STATUS.md`, then commit your branch.
