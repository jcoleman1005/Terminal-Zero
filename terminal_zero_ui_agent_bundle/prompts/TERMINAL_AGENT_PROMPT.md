# Terminal Core Agent Prompt

You are Agent A: Terminal Core.

Read:
- `docs/ui/TERMINAL_ZERO_UI_SPEC.md`
- `docs/ui/CONTRACTS.md`
- `docs/ui/AGENT_TERMINAL.md`

Work only inside your assigned terminal directories.

Implement ONLY the first Terminal milestone:
- standalone terminal test scene
- authentic prompt presentation
- command entry
- Enter submission
- committed prompt+command scrollback
- fake backend output
- fresh prompt
- basic mouse-wheel scrollback

Do not add CRT effects, companion UI, telemetry, or unrelated game logic.

Do not modify shared contracts.

If the shared contract blocks implementation, create a contract-change request in your owned test/docs area and continue everything else possible.

Run the acceptance tests in `AGENT_TERMINAL.md`, then commit your branch.
